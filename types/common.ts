import { CSSProperties, ReactNode } from 'react';
export type FlexProps = {
    align?: string;
    // children?: ReactNode;
    className?: string;
    column?: boolean;
    flex?: string;
    gap?: number;
    justify?: string;
    style?: CSSProperties;
};
